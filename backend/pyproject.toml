[project]
name = "otaku-shelf-backend"
version = "0.0.1"
requires-python = "~=3.12.0"
dependencies = [
    "fastapi[standard]",
    "supertokens-python",
    "sqlmodel",
    "pydantic-settings",
]

[dependency-groups]
dev = ["ruff"]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
